test:
    override:
      - node_modules/.bin/istanbul cover --report lcovonly --print none -x **/test-helpers/** node_modules/.bin/_mocha -- -R xunit > $CIRCLE_TEST_REPORTS/mocha/tests.xml && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage